doctype html
html
  head
    style.
      body {
        font-family: Arial, sans-serif;
        margin: 20px;
        padding: 20px;
      }
      h3 {
        text-align: center;
      }
      table {
        width: 100%;
        border-collapse: collapse;
        margin-bottom: 20px;
      }
      th, td {
        border: 1px solid #dddddd;
        text-align: left;
        padding: 10px;
      }
      th {
        background-color: #f2f2f2;
      }
      .total {
        font-weight: bold;
        text-align: right;
      }

  body
    h3 Café Restobar Management System

    table
      tr
        th(colspan="2") Detalles del cliente
      tr
        td Nombre: #{name}
        td Email: #{email}
      tr
        td Número de contacto: #{contactNumber}
        td Método de pago: #{paymentMethod}

    h3 Detalles del producto:
    table
      tr
        th Nombre
        th Categoría
        th Cantidad
        th Precio
        th Sub Total
      each product in productDetails
        tr
          td #{product.name}
          td #{product.category}
          td #{product.quantity}
          td $#{product.price.toFixed(2)}
          td $#{product.total.toFixed(2)}

    table
      tr
        td.total(colspan="4") Total:
        td.total $#{totalAmount.toFixed(2)}

    h3 Gracias por su visita. Vuelva pronto.!
