doctype html
html
  head
    style.
      body {
        font-family: Arial, sans-serif;
        margin: 20px;
        padding: 20px;
      }
      h3 {
        text-align: center;
      }
      table {
        width: 100%;
        border-collapse: collapse;
        margin-bottom: 20px;
      }
      th, td {
        border: 1px solid #dddddd;
        text-align: left;
        padding: 10px;
      }
      th {
        background-color: #f2f2f2;
      }
      .total {
        font-weight: bold;
        text-align: right;
      }

  body
    h3 Café Management System

    table
      tr
        th(colspan="2") Customer Details
      tr
        td Name: #{name}
        td Email: #{email}
      tr
        td Contact Number: #{contactNumber}
        td Payment Method: #{paymentMethod}

    h3 Product Details:
    table
      tr
        th Name
        th Category
        th Quantity
        th Price
        th Sub Total
      each product in productDetails
        tr
          td #{product.name}
          td #{product.category}
          td #{product.quantity}
          td $#{product.price.toFixed(2)}
          td $#{product.total.toFixed(2)}

    table
      tr
        td.total(colspan="4") Total:
        td.total $#{totalAmount.toFixed(2)}

    h3 Gracias por su visita. Vuelva pronto.!
